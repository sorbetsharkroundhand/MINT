# MINT — 프로젝트 헌법 & Claude Code 작업 가이드

**Writing Platform, Fiction First.** 한글 장편 소설에 깊이 특화된 로컬 **글쓰기 플랫폼**
(macOS, SwiftUI + AppKit + MLX). AI 없이도 글쓰기의 기본 기능을 쓸 수 있어야 한다.
아키텍처·로드맵은 [PLAN.md](PLAN.md), 사용자 안내는 [README.md](README.md).

> **"AI는 다음 텍스트를 맞히는 기계가 아니다. 문서를 살아있는 세계로서
> 계속 이해하고, 그 이해로부터 예측하는 공저자다."**

이 문서는 프로젝트의 철학과 불변 규칙(헌법)이다. 모든 설계·구현 판단은
여기에 어긋나지 않아야 하며, 어긋나야 한다면 이 문서를 먼저 고친다.

## 0. 빌드 / 실행 (중요 — 매 세션 필요)

- **컴파일 확인**: `swift build` — 타입 체크용으로는 충분하다.
- **실행은 `swift build` 산출물로 하면 크래시** ("Failed to load the default
  metallib") — SwiftPM CLI는 MLX Metal 셰이더를 빌드하지 못한다.
  - 해결: `scripts/prepare-metallib.sh` 1회 실행 (xcodebuild로 metallib 생성·복사)
    → 이후 `swift run MINT` 동작. mlx-swift 버전을 올리면 다시 실행.
- 벤치 CLI: `swift run -c release MINTBench` (동일한 metallib 제약).

## 1. 제품 철학 (모든 결정의 뿌리)

1. **범용 글쓰기 플랫폼, 소설 우선 특화.** 상위 도메인은 `WritingProject`이며,
   일반 글쓰기와 소설을 `WritingMode`로 구분한다. General은 독립적인 기본 경로이고,
   장편 서사의 일관성·인물·시간선은 Fiction 특화 계층에서 다룬다.
2. **품질 > 적극성.** 나쁜 제안 하나가 좋은 제안 열 개의 신뢰를 깎는다.
   확신이 없으면 침묵이 정답이다.
3. **완전 로컬.** 원고는 절대 기기를 떠나지 않는다. 네트워크 추론·원격 텔레메트리
   금지. 품질 지표도 로컬에만 기록한다.
4. **macOS 네이티브의 이점을 쓴다.** 상주 앱이므로 백그라운드 이해·증분 인덱싱·
   캐시 프리웜이 가능하다 — "예측 시점에 모든 걸 다시 만드는" 아키텍처 금지.
5. **기억은 사용자의 것.** AI가 문서에서 이해한 모든 것(인물·사건·요약)은
   사용자가 볼 수 있고 고칠 수 있어야 한다. 사용자 정본(User Canon)·수정은
   내구 데이터이며, 항상 자동 추출을 이기고 파생 캐시 재구축으로 지워지지 않는다.

## 2. 아키텍처 원칙

1. **원고와 사용자 결정은 보존한다.** 자동 파생 지식(요약·바이블·사건·문체 프로필)은
   원문에서 재계산 가능한 캐시다. 파생 스키마가 바뀌면 버전을 올리고 백그라운드에서
   재구축한다. 사용자 원고·정본은 별도로 보존하며, 저장 형식 전환은 원본을 남기고 검증한다.
2. **백그라운드가 이해를 준비하고, 예측은 조립만 한다.** 예측 경로에서 LLM
   다중 호출·인덱스 재구축·디스크 스캔 금지. 예측은 준비된 지식의 랭킹·조립 +
   단일 생성 호출이다.
3. **증분이 기본.** 전체 재처리는 복구 수단이지 정상 경로가 아니다. 콘텐츠
   해시로 더티 씬만 다시 읽고, 요약은 더티 경로만 상향 전파한다.
4. **시점(타임라인) 인지.** 지식은 작품 내 위치(장·절·블록)로 버전된다.
   커서 위치 **이후**의 지식은 프롬프트에 주입하지 않는다 — 3장을 고치는 중에
   9장의 결말이 새어 들어가면 안 된다.
5. **결정적 로직 우선.** 헤딩 파싱·빈도 통계·정규식으로 되는 일에 LLM 토큰을
   쓰지 않는다. LLM은 요약·프로파일링·사건 추출처럼 이해가 필요한 곳에만.
6. **단일 모델, 예측 최우선.** 모델은 하나만 상주한다. 예측 요청은 진행 중인
   백그라운드 생성을 즉시 선점(취소)한다.
7. **측정 없이 튜닝 없음.** 품질·지연에 대한 주장은 MINTBench 수치 또는
   재현 가능한 E2E 절차로 뒷받침한다. 감으로 파라미터를 바꾸지 않는다.

## 3. 예측 행동 규범 (UX 불변식)

- **게이트 순서 유지**: 마스터 스위치 → IME 조합(`hasMarkedText`) → 문단 끝 →
  컨텍스트 길이 → 디바운스. **한글 조합 중 트리거는 절대 금지.**
- **지연 예산**: 첫 고스트 글자까지 모드별 목표는 PLAN §10. 예산 초과가 예상되면
  지식 블록을 줄여서 맞춘다 — UX를 늦추는 쪽으로 풀지 않는다.
- **일관성 > 유창성**: 스토리 바이블과 모순되는 제안(죽은 인물의 등장, 말투 붕괴)은
  유창해도 실패다. 예측 우선순위: 일관성 유지 > 다음 대사 > 다음 문장 > 다음 문단 > 다음 단어.
- **인물 등록은 신뢰도 기반** — 구조 신호(유정 격조사·발화 귀속·호명)가 겹겹인
  HIGH 후보만 자동 등록하되 표식과 1클릭 삭제를 보장한다. MEDIUM 이하와
  별칭 병합은 **항상 사용자 확인** (PLAN §7).
- **고스트는 조용히**: 화면을 흔들거나 포커스를 뺏는 UI 금지. 제안은 나타나고,
  거절되면 흔적 없이 사라진다.

## 4. 개발 규칙

- 주석·UI 문자열은 **한국어**. 주석은 "왜"를 적는다 (PLAN 섹션 참조 형식: `PLAN §N`).
- 색·폰트는 반드시 `Theme.swift`의 `MintTheme`/`MintFonts` 토큰 (라이트/다크 두 벌).
  하드코딩 색 금지. 토글 UI는 `ContentView.swift`의 `GlassSwitch` 재사용.
- 설정은 `CompletionSettings`(UserDefaults, `completion.*` 키)에 추가하고,
  추론 쪽엔 `CompletionParameters` 스냅샷만 넘긴다 (actor 격리 경계).
- **지식 로직의 자리**: 추출·요약·바이블·타임라인은 `Sources/MINTCore/Knowledge/`에.
  에디터(`BlockTextView`, 이미 3천 행)나 `EntryStore`에 섞지 않는다.
- **플랫폼 도메인의 자리**: `Sources/MINTCore/Project/`. 범용 프로젝트·문서 타입은
  Fiction 지식 타입에 의존하지 않는다. 파생 씬 해시를 영구 문서 ID로 쓰지 않는다.
- **백그라운드 작업 3요건**: ① 취소 협조(`Task.isCancelled`) ② 열·저전력 게이트
  (`ProcessInfo.thermalState`, 저전력 모드) ③ 콘텐츠 해시 메모이제이션(같은 입력
  재처리 금지). 셋 중 하나라도 빠진 백그라운드 작업은 머지하지 않는다.
- 트렁크 기반: `main`은 항상 빌드되게, 기능은 단기 브랜치(`feat/`·`fix/`·`spike/`) 후 머지.

## 5. 새 아이디어 평가 체크리스트

기능·아키텍처 제안은 채택 전에 다섯 질문을 통과해야 한다.
하나라도 "아니오"면 PLAN §16(열린 질문)에 근거와 함께 보류로 기록한다.

1. 예측 품질(또는 **토큰당 품질**)이 측정 가능하게 오르는가?
2. 작업이 예측 시점이 아니라 **백그라운드**로 가는가?
3. **증분 갱신**이 가능한가 — 전체 재구축 없이 유지되는가?
4. MINTBench 또는 E2E 절차로 **측정**할 수 있는가?
5. 실패해도 **원문이 안전**한가 (파생 캐시만 버리면 복구되는가)?

통념(거대 컨텍스트·범용 벡터 RAG·예측 시점 에이전트 체인)은 기본값이 아니다 —
온디바이스·소설 도메인에서의 비판적 검토는 PLAN §1 참조.

## 6. 검증

- 변경 후 최소 `swift build` 통과 확인.
- UX 변경은 실행해서 확인: 제안 등장(~500ms) · 조합 중 미등장 · Tab/→/Esc ·
  재실행 시 저널 보존 (`~/Documents/MINT/entries.json`).
- 예측 품질·지연 변경은 MINTBench 수치를 PR·커밋 메시지에 남긴다 (PLAN §13).

## 7. 문서 유지 규칙 (살아있는 문서)

- **CLAUDE.md** = 철학·불변 규칙. 간결 유지(≤ ~160행) — 매 세션 로드되므로
  이 문서 자체가 토큰 예산 대상이다. 상세는 PLAN으로 위임한다.
- **PLAN.md** = 아키텍처·시스템 설계·로드맵. 구조가 바뀌는 변경은 **같은 커밋에서**
  PLAN을 갱신한다. 큰 분석·실험 기록은 `docs/`에 두고 PLAN에서 링크한다.
- 두 문서는 항상 동기화: 철학이 바뀌면 CLAUDE.md 먼저, 설계가 바뀌면 PLAN 먼저 —
  서로 모순된 채 머지하지 않는다.
- PLAN 섹션 번호는 코드 주석의 앵커다 — 재배치 시 PLAN 부록 A(구판 참조 색인)를 갱신한다.
- 더 나은 아키텍처를 발견하면 이 문서들을 고치는 것이 곧 구현의 첫 단계다.
  요구사항은 만족의 대상이 아니라 개선의 대상이다.
